# Changelog

## 1.1.0

Map view and tracking station.

- **Custom map node icon.** Bennu now draws as its own flat-poled diamond rather than a
  generic circle. Generated by `Tools/Generate-BennuIcon.ps1` from the same shape law as
  the body itself, with its proportions pushed so it survives at the ~20 px the icon is
  actually drawn at.
- **You can zoom in.** `maxZoom` was at Kopernicus's 60,000 m default, which is sensible
  for a planet and absurd for a body 2.3 km across — the camera stopped several km out and
  Bennu never grew past a few percent of the screen. Now 2,500 m, which frames the whole
  body and still leaves most of a body radius of camera clearance.
- **Fixed the flat, glossy look at distance.** The scaled-space material was running
  Hapke backscatter at 0.85, *above* the terrain's 0.6. Hapke is precisely the term that
  removes limb darkening, so as Bennu shrank the terminator flattened out and it read as a
  smooth, evenly-lit pebble even with specular at zero. Scaled now matches terrain, so the
  body is lit the same way at every distance. Scaled-space normal strength also raised
  1.0 → 1.25 so relief survives the mip chain as the body gets small.
- Validator: recognises `iconTexture`, and no longer reports Parallax's terrain and
  scatter textures as missing. Parallax now ships those inside `.unity3d` bundles rather
  than as loose files, and its own configs reference the old paths exactly as this pack
  does — so those seven errors were the check being stale, not a broken reference.

## 1.0.0

First public release.

- 101955 Bennu as a Kopernicus `CelestialBody` — landable terrain with colliders, thirteen
  biomes with their own science text, and a real orbit crossing Kerbin's.
- Procedurally generated height, colour, normal and biome maps, reproducible byte-for-byte
  from `Tools/Generate-BennuMaps.ps1`.
- Compatibility patches for Parallax Continued, PlanetShine, KerbalFX and SCANsat, plus a
  full stock + CRP resource distribution with per-biome overrides. All gated behind
  `:NEEDS[…]`.
- `Tools/Validate-Bennu.ps1` static validator over the whole pack.

Fixes applied after the first flight test — unlandable terrain from 8-bit height
quantisation, over-dense brown scatters, and specular gloss in the tracking station — are
detailed in [docs/TECHNICAL.md](docs/TECHNICAL.md).
